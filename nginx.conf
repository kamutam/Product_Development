server {
    listen 80;
    
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # This is critical for React Router to work!
        # It tells Nginx to route all requests back to index.html
        try_files $uri $uri/ /index.html;
    }
}
